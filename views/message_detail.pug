extends layout

block content
  h1 #{message.title}

  if (currentUser && currentUser.secret == "secret" || currentUser && currentUser.admin == true)
    p #[strong Posted by: ]
      a(href=message.user.url) #{message.user.name}
    p #[strong Date:] #{message.timestamp_formatted}
  p #[strong Message:] #{message.text}

  if (currentUser && currentUser.id == message.user._id || currentUser && currentUser.admin == true)
    hr
    p
      a(href=message.url+'/delete') Delete Message
  if (currentUser && currentUser.id == message.user._id)
    p
      a(href=message.url+'/update') Edit Message